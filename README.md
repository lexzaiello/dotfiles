# dotfiles
My dotfiles.

This repo exposes a NixOS system configuration via `.nixosModules.default`. See the screenshot below for my rice:

![rice screenshot](./.github/static/rice.png)

Correctness of the build is checked via  `check` in the flake, which is run via CI on every push.
